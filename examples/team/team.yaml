apiVersion: ldap.kubed.io/v1alpha1
kind: Team
metadata:
  name: staff
spec:
  baseDn: ou=teams,dc=example
  description: Internal staff
  owner: uid=alice,ou=users,dc=example   # seed member; real membership filled externally
