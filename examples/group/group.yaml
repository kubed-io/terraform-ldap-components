apiVersion: ldap.kubed.io/v1alpha1
kind: Group
metadata:
  name: kubed-test-group
spec:
  baseDn: ou=groups,dc=burbs
  gidNumber: "9020"
  description: Kubed test posix group (mixes a human and a service account)
  members:                 # curated bare uids (users and/or service accounts)
  - kelly                  # existing human in ou=users
  - kubed-test-sa          # the service account from the service-account example
